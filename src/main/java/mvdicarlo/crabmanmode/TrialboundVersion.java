package mvdicarlo.crabmanmode;

/**
 * Bump on every change the group ships. Shown in the panel title and startup
 * log, and exchanged in the party handshake so version mismatches between
 * members are called out in chat instead of causing mystery asymmetries.
 */
public final class TrialboundVersion {
    public static final String VERSION = "0.3.10";

    private TrialboundVersion() {
    }
}
